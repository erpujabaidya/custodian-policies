policies:
  - name: account-cloudtrail-enabled
    resource: account
    region: us-east-2
    filters:
      - type: check-cloudtrail
        global-events: true
        multi-region: true
        running: true
